# minio-hive-trino

## Como Iniciar

**Passo 1:** Navegue até o diretório do projeto:

```bash
cd minio-hive-trino
```

**Passo 2:** Inicie os serviços utilizando o Docker Compose:

```bash
docker-compose up -d
```
